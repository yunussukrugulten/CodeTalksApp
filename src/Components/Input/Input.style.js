import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderBottomWidth: 1,
    borderColor: '#fff',
    marginBottom: 10,
  },
  input: {
    color: '#fff',
    padding: 15,
    fontSize: 15,
  },
});
